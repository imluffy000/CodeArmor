# Copy to frontend/.env.local for development, and set the same key in your
# Vercel project settings for Production and Preview.
#
# Everything prefixed VITE_ is inlined into the public JavaScript bundle at
# build time. Never put a secret behind this prefix.
VITE_API_URL=http://127.0.0.1:8000
